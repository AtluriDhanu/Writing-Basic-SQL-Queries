# Writing-Basic-SQL-Queries
This task involves writing SQL queries to retrieve data from the library database using SELECT statements. It includes filtering with WHERE, combining conditions using AND/OR, pattern matching with LIKE, range queries using BETWEEN, sorting with ORDER BY, and limiting results with LIMIT.
